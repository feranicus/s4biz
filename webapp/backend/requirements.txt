# PINS ARE A SUPPLY-CHAIN POSITION, NOT HOUSEKEEPING.
#
# fastapi 0.115 capped starlette below 0.47, which meant three HIGH, FIXABLE starlette advisories
# could not be resolved at all: our own requirement file was the thing blocking the patch. The
# scanner was right and the pin was wrong. fastapi 0.141 removes the cap.
#   CVE-2026-48818  SSRF and credential theft via UNC paths in StaticFiles  (fixed 1.1.0)
#   CVE-2025-62727  denial of service via Range header merging             (fixed 0.49.1)
#   CVE-2026-54283  form() limits silently ignored                         (fixed 1.3.1)
# The first is not theoretical here: main.py mounts StaticFiles on /assets.
fastapi==0.141.*
starlette>=1.3.1
uvicorn[standard]==0.30.*
pydantic==2.*
# Outbound notification of a contact enquiry (Gmail API). SMTP is blocked outbound on this
# droplet, so do not "fix" this to smtplib: it would silently stop every enquiry reaching a human.
google-auth==2.*
requests==2.*
